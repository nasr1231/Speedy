@model IEnumerable<UserViewModel>
@{
    ViewData["Title"] = "System Users";
}

@section Styles {

    <link href="~/assets/plugins/datatables/datatables.bundle.css" rel="stylesheet" crossorigin="anonymous" />
}

<div class="card shadow-sm mb-5 mb-lg-10">
    <div class="card-header">
        <div class="card-title">
            <div class="symbol symbol-70px me-4">
                <div class="symbol-label fs-2 fw-semibold text-success">
                    <i class="fa-solid fa-truck-fast text-primary"></i>
                </div>
                <span class="symbol-badge badge badge-circle bg-primary start-100 text-white"></span>
            </div>
            <h2 class="fw-bolder">Delivery Agents</h2>
        </div>
        <div class="card-toolbar my-7">
            <a class="btn btn-sm btn-light btn-primary fs-4 fw-bolder" asp-action="Create">
                <i class="bi bi-plus-square-dotted"></i>
                Add System User
            </a>
        </div>
    </div>
</div>

<div class="card shadow-sm mb-5 mb-lg-10 min-h-md-425px">
    <div class="card-body pt-0">
        <div class="table-responsive" style="min-height: 270px">
            <partial name="_TableHeader" />
            <table class="table table-row-bordered table-row-gray-300 gy-3 align-middle js-datatables" data-export-title="Delivery Agents">
                <thead>
                    <tr class="fw-bold fs-6 text-gray-800">
                        <th>Id</th>
                        <th>UserName</th>
                        <th>Email</th>                        
                        <th>Registration Date</th>
                        <th>Update Date</th>
                        <th>Status</th>
                        <th class="js-no-export">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach(var user in Model)
                    {
                        <partial name="_RowData" model="@user"/>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Plugins {
    <script src="~/assets/plugins/datatables/datatables.bundle.js"></script>
    <script src="~/assets/plugins/datatables/vfs_fonts.js"></script>
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}